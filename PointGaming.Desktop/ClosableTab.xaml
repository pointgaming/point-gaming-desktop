<TabItem x:Class="PointGaming.Desktop.ClosableTab"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:my="clr-namespace:PointGaming.Desktop"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <TabItem.HeaderTemplate>
        <DataTemplate DataType="{x:Type TabItem}">
            <ContentPresenter>
                <ContentPresenter.Content>
                    <my:ClosableTabHeader x:Name="ClosableHeader"
                                          Text="{Binding RelativeSource={RelativeSource AncestorType={x:Type TabItem} }, Path=Header}"
                                          ShouldFlash="{Binding RelativeSource={RelativeSource AncestorType={x:Type TabItem} }, Path=ShouldFlash}"/>
                </ContentPresenter.Content>
            </ContentPresenter>
        </DataTemplate>
    </TabItem.HeaderTemplate>
</TabItem>
